// table with id age-table 
let table = document.getElementById("age-table");

// label elements inside the table 
let label = table.getElementsByTagName("label");

// first td in the table with word age 
table.getElementsByTagName('td')[0];

// form with name search 
let search = document.getElementsByName('search')[0];

// first input 
let input = document.querySelector("input");

// last input 
let lastInput= document.querySelectorAll("input");
lastInput[lastInput.length-1]